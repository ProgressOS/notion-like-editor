import React from "react";
import { TooltipProps } from "./types";
export declare const Tooltip: ({ children, enabled, title, shortcut, tippyOptions, }: TooltipProps) => React.JSX.Element;
export default Tooltip;
