import React from 'react';
import { Editor } from '@tiptap/react';
export type ContentItemMenuProps = {
    editor: Editor;
};
export declare const ContentItemMenu: ({ editor }: ContentItemMenuProps) => React.JSX.Element;
